<Window x:Class="Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client"
        xmlns:viewmodel="clr-namespace:Client.MVVM.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="472" Width="800" WindowState="Maximized">

    <Window.DataContext>
        <viewmodel:MainViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MaxWidth="150"/>
            <ColumnDefinition Width="*" MaxWidth="200"/>
            <ColumnDefinition Width="*" MaxWidth="200"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Grid.Column="0">
            <TextBox x:Name="chatNameTextBox" Text="{Binding ChatName}"></TextBox>
            <TextBox x:Name="chatDescTextBox" Text="{Binding ChatDesc}"></TextBox>
            <Button x:Name="createChatButton" Command="{Binding CreateChatCommand}" Height="31"></Button>
        </StackPanel>
        <TextBox x:Name="messageTextBox" Margin="10,332,73,-367" Text="{Binding Message}" Grid.ColumnSpan="2" Grid.Column="1" Grid.Row="2"></TextBox>
        <Button x:Name="sendButton" Content="Send" Command="{Binding SendMessageCommand}" Margin="200,332,-150,-357" Grid.Column="2" Grid.Row="2"/>
        <ListBox x:Name="chatMembersListbox" Grid.Column="2" Margin="350,0,-250,-226" Grid.RowSpan="3" ItemsSource="{Binding ChatMembers}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="{Binding Username}" Grid.Row="0"></TextBlock>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <ListBox x:Name ="messagesListbox" Grid.Column="1" Margin="5,0,-145,-310" MaxHeight="300" Grid.RowSpan="3" Grid.ColumnSpan="2" ItemsSource="{Binding ChatMessages}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Sender}" Grid.Row="0"></TextBlock>
                        <TextBlock TextWrapping="Wrap" Width="250"   Text="{Binding Content}" Grid.Row="1"></TextBlock>
                        <TextBlock Text="{Binding Date}" Grid.Row="1" Grid.Column="1" Margin="400 0 0 0" HorizontalAlignment="Right"></TextBlock>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <ListBox Margin="350,231,-240,-357" ItemsSource="{Binding Chats}" Grid.Column="2"  MaxHeight="150" Grid.Row="2">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid VerticalAlignment="Stretch">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="{Binding Name}" Grid.Row="0"></TextBlock>
                        <TextBlock Text="{Binding Description}" Grid.Row="1"></TextBlock>
                        <Button Content="Join" Command="{Binding DataContext.JoinChatCommand, RelativeSource={RelativeSource AncestorType={x:Type ListBox}}}" CommandParameter="{Binding Id}" Grid.Row="2"></Button>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <ListBox x:Name="userChatsListbox" ItemsSource="{Binding UserChats}" Margin="0,39,0,-357" Grid.Row="2"  SelectedItem="{Binding SelectedChat}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Name}" Grid.Row="0" Grid.Column="0"></TextBlock>
                        <Button Content="Leave" Command="{Binding DataContext.LeaveChatCommand, RelativeSource={RelativeSource AncestorType={x:Type ListBox}}}" CommandParameter="{Binding Id}" Grid.Row="1" Grid.Column="0"></Button>
                        <Border CornerRadius="50" Grid.Row="0" Grid.Column="1" Margin="0 5 10 0"
        Width="30"
        Height="30"
        Background="Red" 
        BorderThickness="0">
                            <TextBlock HorizontalAlignment="Center" Margin="0 5 0 0" Text="{Binding Id}"/>
                        </Border>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>

        </ListBox>
    </Grid>
</Window>
